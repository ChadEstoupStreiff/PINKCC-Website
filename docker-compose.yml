services:
  web:
    build: ./web
    ports:
      - "8080:80"
    restart: always
    container_name: pinkcc-website
    networks:
      - pinkcc-network
    
  # api:
  #   build: ./api
  #   ports:
  #     - "8081:5000"
  #   restart: always
  #   container_name: pinkcc-api
  #   networks:
  #     - pinkcc-network
      
networks:
  pinkcc-network:
    driver: bridge